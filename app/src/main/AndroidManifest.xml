<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.github.gao23.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".UI.NewTipActivity"
            android:theme="@style/tab_theme"/>
        <activity android:name=".UI.computerEditActivity" />
        <activity android:name=".UI.cashEditActivity" />
        <activity android:name=".UI.calculateActivity" />
        <activity android:name=".UI.AboutActivity"></activity>
    </application>

</manifest>